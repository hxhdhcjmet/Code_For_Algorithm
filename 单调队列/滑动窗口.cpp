#include<iostream>
#include<deque>
#include<vector>
using namespace std;
auto maxWindowData(vector<int>& nums,int k){
    vector<int> result;
    deque<int> window;
    for(int i=0;i<nums.size();i++){
        while(!window.empty()&&)
    }

}